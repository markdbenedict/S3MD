The project has three main compnents:
1. an MD that Does a full N^2 Stillinger-Weber Si simulation in Python
2. an MD that does the same simulation using
    a symetry adapted ANN instead to replace the SW potential
3. an MD that does same simulation with GPU enabled ANN

testing commit capabilities on PC
Correcting for time errors
Username not showing up?
trying to figure it out still